import CardCaptureForm from "./CardCaptureForm";

export const metadata = {
  title: "Secure Card Capture · Humanity & Blessings",
  robots: { index: false, follow: false },
};

export default function CardCapturePage() {
  return <CardCaptureForm />;
}
